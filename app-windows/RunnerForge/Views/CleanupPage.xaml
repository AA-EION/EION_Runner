<UserControl x:Class="RunnerForge.Views.CleanupPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="820">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Cleanup" FontSize="22" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="12,4"/>
            <Button Content="Purge now" Command="{Binding PurgeCommand}" Padding="12,4" Margin="8,0,0,0"/>
            <CheckBox Content="Purge on exit" IsChecked="{Binding PurgeOnExit}"
                      VerticalAlignment="Center" Margin="16,0,0,0"/>
            <TextBlock Text="{Binding Status}" Margin="16,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" Margin="0,0,8,0">
                <StackPanel DockPanel.Dock="Top">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="KEEP" FontWeight="Bold" FontSize="15"/>
                        <TextBlock Text="{Binding TotalKept}" Margin="10,0,0,0" Foreground="#555"/>
                    </StackPanel>
                    <TextBlock Text="{Binding KeepExplanation}" TextWrapping="Wrap" Foreground="#666"
                               Margin="0,4,0,8" FontSize="11"/>
                </StackPanel>
                <ListBox ItemsSource="{Binding KeepItems}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding HumanBytes}" Foreground="#666" FontSize="11"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <DockPanel Grid.Column="1" Margin="8,0,0,0">
                <StackPanel DockPanel.Dock="Top">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="PURGE" FontWeight="Bold" FontSize="15"/>
                        <TextBlock Text="{Binding TotalReclaimable}" Margin="10,0,0,0" Foreground="#555"/>
                    </StackPanel>
                    <TextBlock Text="Deleted on close and on demand. Containers, job workspaces, temp downloads, dangling layers and old logs."
                               TextWrapping="Wrap" Foreground="#666" Margin="0,4,0,8" FontSize="11"/>
                </StackPanel>
                <ListBox ItemsSource="{Binding PurgeItems}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding HumanBytes}" Foreground="#666" FontSize="11"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
