<UserControl x:Class="RunnerForge.Views.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="820">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Logs" FontSize="22" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Margin="0,0,0,12" Foreground="#666"
                   Text="Every line here has already passed the redactor. Known secret values are replaced with *** before a line reaches this list, a file, or the clipboard."/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Source" VerticalAlignment="Center"/>
            <TextBox Text="{Binding SourceFilter, UpdateSourceTrigger=PropertyChanged}" Width="160" Margin="8,0,16,0"/>
            <TextBlock Text="Minimum level" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Levels}" SelectedItem="{Binding MinimumLevel}"
                      Width="110" Margin="8,0,16,0"/>
            <Button x:Name="CopyButton" Content="Copy" Padding="12,4" Click="OnCopyClick"/>
            <Button x:Name="SaveButton" Content="Save to file" Padding="12,4" Margin="8,0,0,0" Click="OnSaveClick"/>
            <Button Content="Clear" Command="{Binding ClearCommand}" Padding="12,4" Margin="8,0,0,0"/>
            <TextBlock Text="{Binding CopyStatus}" VerticalAlignment="Center" Margin="12,0,0,0"
                       Foreground="#666" TextTrimming="CharacterEllipsis"/>
        </StackPanel>

        <ListBox ItemsSource="{Binding Entries}" FontFamily="Consolas" FontSize="12"
                 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
    </DockPanel>
</UserControl>
